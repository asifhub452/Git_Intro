function greet(name) {
  console.log("hello " + name);
}
greet("Asif");
// we use git diff to check the differences in files
// only for the files which are not added or unstaged
// once stagged won't show differencea
